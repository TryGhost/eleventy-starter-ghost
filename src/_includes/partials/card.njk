{% macro card(props) %}
  <article class="card">
    <a class="card__anchor" href="{{ props.url }}">
      <img class="card__image" src="{{ props.image }}" alt="{{ props.title }}">
      <header class="card__header">
        <h2 class="card__title">{{ props.title }}</h2>
        <p>{{ props.description }}</p>
      </header>
    </a>
  </article>
{% endmacro %}
